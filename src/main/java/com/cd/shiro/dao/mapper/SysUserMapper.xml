<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.shiro.dao.SysUserDao">
    <select id="findByUserName" parameterType="string" resultType="com.cd.shiro.pojo.domain.SysUser">
     SELECT
		sys_user.user_id userId,
		sys_user.user_name userName,
		sys_user.full_name fullName,
		sys_user.PASSWORD password,
		sys_user.salt  salt
	FROM
		`sys_user`
		 where su.user_name=#{userName}
    </select>
</mapper>
