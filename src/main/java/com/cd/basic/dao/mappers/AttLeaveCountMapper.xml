<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cd.basic.dao.AttLeaveCountDao">
    <!-- AttLeaveCount的resultMap,column是给数据库列起的别名,它对应property类的属性-->
    <resultMap id="result_AttLeaveCount_Map" type="com.cd.basic.pojo.domain.AttLeaveCount">
        <id column="id" property="id" />
        <result column="USER_FK_CODE" property="userFkCode" />
        <result column="LEAVE_TIME" property="leaveTime" />
        <result column="LEAVE_TYPE" property="leaveType" />
        <result column="DEL_STATUS" property="delStatus" />
        <result column="LEAVE_DATE" property="leaveDate" />
        <result column="CREATE_TIME" property="createTime" />
        <result column="UPDATE_TIME" property="updateTime" />
        <result column="SINGLE_FK_CODE" property="singleFkCode" />
        <result column="FK_CODE" property="fkCode" />
    </resultMap>

	   <!-- Assist用于辅助工具类setRequires设置的条件,${req.require}表示列名,#{req.value}表示值它是防SQL注入的 -->
    <sql id="Assist">
        <where>
            <foreach collection="require" item="req" separator=" ">
                ${req.require} #{req.value} <if test="req.suffix != null"> ${req.suffix}</if>
            </foreach>
        </where>
    </sql>

	   <!-- updateAssist用于更新数据的使用它拓展自Assist,用于辅助工具类setRequires设置的条件,${req.require}表示列名,#{req.value}表示值它是防SQL注入的 -->
    <sql id="updateAssist">
        <where>
            <foreach collection="assist.require" item="req" separator=" ">
                ${req.require} #{req.value} <if test="req.suffix != null"> ${req.suffix}</if>
            </foreach>
        </where>
    </sql>

    <!-- 数据库中表名为:Att_Leave_Count的列名,as前是数据库的列明,as后是列的别名用于映射成实体类中的属性,需要注意的是别名必须与resultMap中的column别名一致 -->
    <sql id="Att_Leave_Count_Column">
        Att_Leave_Count.id as id
        ,Att_Leave_Count.USER_FK_CODE as USER_FK_CODE
        ,Att_Leave_Count.LEAVE_TIME as LEAVE_TIME
        ,Att_Leave_Count.LEAVE_TYPE as LEAVE_TYPE
        ,Att_Leave_Count.DEL_STATUS as DEL_STATUS
        ,Att_Leave_Count.LEAVE_DATE as LEAVE_DATE
        ,Att_Leave_Count.CREATE_TIME as CREATE_TIME
        ,Att_Leave_Count.UPDATE_TIME as UPDATE_TIME
        ,Att_Leave_Count.SINGLE_FK_CODE as SINGLE_FK_CODE
        ,Att_Leave_Count.FK_CODE as FK_CODE
    </sql>

    <!--获得类名为:AttLeaveCount对应的数据库表的数据总行数 -->
    <select id="getAttLeaveCountRowCount" parameterType="com.cd.common.Assist" resultType="java.lang.Long">
        select count(id) from Att_Leave_Count
        <if test="require!=null"><include refid="Assist" /></if>
    </select>
    <!-- 获得类名为:AttLeaveCount对应数据库中表的数据集合 -->
    <select id="selectAttLeaveCount" parameterType="com.cd.common.Assist"  resultMap="result_AttLeaveCount_Map">
        select  <if test="distinct !=null">${distinct}</if>
        <choose>
            <when test="resultColumn!=null">${resultColumn}</when>
            <otherwise>
        <include refid="Att_Leave_Count_Column" /> 
            </otherwise>
        </choose>
        from Att_Leave_Count
        <choose>
            <when test="rowSize ==null">
                <if test="require!=null">
                    <include refid="Assist" />
                </if>
                <if test="order !=null">${order}</if>
            </when>
            <otherwise>
                inner join
                (
                SELECT id as primary_key FROM Att_Leave_Count
                <if test="require!=null">
                    <include refid="Assist" />
                </if>
                <if test="order !=null">${order}</if>
                <choose>
                    <when test="startRow !=null and rowSize !=null">LIMIT #{startRow},#{rowSize} </when>
                    <when test="rowSize !=null">LIMIT #{rowSize}</when>
                 </choose>
                 ) as tmp
                 on tmp.primary_key=Att_Leave_Count.id
            </otherwise>
        </choose>
    </select> 

    <!-- 获得一个AttLeaveCount对象,以参数AttLeaveCount对象中不为空的属性作为条件进行查询-->
    <select id="selectAttLeaveCountByObj" parameterType="com.cd.basic.pojo.domain.AttLeaveCount" resultMap="result_AttLeaveCount_Map">
        select 
            <include refid="Att_Leave_Count_Column" /> 
        from Att_Leave_Count
        <where>
            <if test="fkCode != null "> and Att_Leave_Count.FK_CODE = #{fkCode}</if>
            <if test="singleFkCode != null "> and Att_Leave_Count.SINGLE_FK_CODE = #{singleFkCode}</if>
            <if test="updateTime != null "> and Att_Leave_Count.UPDATE_TIME = #{updateTime}</if>
            <if test="createTime != null "> and Att_Leave_Count.CREATE_TIME = #{createTime}</if>
            <if test="leaveDate != null "> and Att_Leave_Count.LEAVE_DATE = #{leaveDate}</if>
            <if test="delStatus != null "> and Att_Leave_Count.DEL_STATUS = #{delStatus}</if>
            <if test="leaveType != null "> and Att_Leave_Count.LEAVE_TYPE = #{leaveType}</if>
            <if test="leaveTime != null "> and Att_Leave_Count.LEAVE_TIME = #{leaveTime}</if>
            <if test="userFkCode != null "> and Att_Leave_Count.USER_FK_CODE = #{userFkCode}</if>
            <if test="id != null "> and Att_Leave_Count.id = #{id}</if>
        </where>
    </select> 

    <!-- 通过AttLeaveCount的id获得对应数据库中表的数据对象-->
    <select id="selectAttLeaveCountById" parameterType="java.lang.Long" resultMap="result_AttLeaveCount_Map">
        select 
            <include refid="Att_Leave_Count_Column" /> 
        from Att_Leave_Count
        where Att_Leave_Count.id = #{id}
    </select> 

    <!-- 将AttLeaveCount插入到对应数据库的表中,包括属性值为null的数据-->

    <insert id="insertAttLeaveCount" parameterType="com.cd.basic.pojo.domain.AttLeaveCount">
        <selectKey keyProperty="id" resultType="Long" order="AFTER">
            SELECT LAST_INSERT_ID() AS id
        </selectKey>
        insert into Att_Leave_Count(id,USER_FK_CODE,LEAVE_TIME,LEAVE_TYPE,DEL_STATUS,LEAVE_DATE,CREATE_TIME,UPDATE_TIME,SINGLE_FK_CODE,FK_CODE) 
        values(#{id},#{userFkCode},#{leaveTime},#{leaveType},#{delStatus},#{leaveDate},#{createTime},#{updateTime},#{singleFkCode},#{fkCode})
    </insert>

    <!-- 将AttLeaveCount中属性值不为null的数据,插入到对应数据库的表中-->
    <insert id="insertNonEmptyAttLeaveCount" parameterType="com.cd.basic.pojo.domain.AttLeaveCount">
        <selectKey keyProperty="id" resultType="Long" order="AFTER">
            SELECT LAST_INSERT_ID() AS id
        </selectKey>
        insert into Att_Leave_Count
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="userFkCode != null">USER_FK_CODE,</if>
            <if test="leaveTime != null">LEAVE_TIME,</if>
            <if test="leaveType != null">LEAVE_TYPE,</if>
            <if test="delStatus != null">DEL_STATUS,</if>
            <if test="leaveDate != null">LEAVE_DATE,</if>
            <if test="createTime != null">CREATE_TIME,</if>
            <if test="updateTime != null">UPDATE_TIME,</if>
            <if test="singleFkCode != null">SINGLE_FK_CODE,</if>
            <if test="fkCode != null">FK_CODE,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null"> #{id},</if>
            <if test="userFkCode != null"> #{userFkCode},</if>
            <if test="leaveTime != null"> #{leaveTime},</if>
            <if test="leaveType != null"> #{leaveType},</if>
            <if test="delStatus != null"> #{delStatus},</if>
            <if test="leaveDate != null"> #{leaveDate},</if>
            <if test="createTime != null"> #{createTime},</if>
            <if test="updateTime != null"> #{updateTime},</if>
            <if test="singleFkCode != null"> #{singleFkCode},</if>
            <if test="fkCode != null"> #{fkCode},</if>
        </trim>
    </insert>

    <!-- 将AttLeaveCount批量插入到对应数据库的表中-->
    <insert id="insertAttLeaveCountByBatch" parameterType="ArrayList">
        <selectKey keyProperty="id" resultType="Long" order="AFTER">
            SELECT LAST_INSERT_ID() AS id
        </selectKey>
        insert into Att_Leave_Count(id,USER_FK_CODE,LEAVE_TIME,LEAVE_TYPE,DEL_STATUS,LEAVE_DATE,CREATE_TIME,UPDATE_TIME,SINGLE_FK_CODE,FK_CODE) values
        <foreach collection="list" item="item" index="index" separator="," >
            (#{item.id},#{item.userFkCode},#{item.leaveTime},#{item.leaveType},#{item.delStatus},#{item.leaveDate},#{item.createTime},#{item.updateTime},#{item.singleFkCode},#{item.fkCode})
        </foreach>
    </insert>

    <!-- 通过AttLeaveCount的id将数据库表中对应的数据删除-->
    <delete id="deleteAttLeaveCountById" parameterType="java.lang.Long">
        delete from Att_Leave_Count
        where id = #{id}
    </delete>

    <!-- 通过辅助工具Assist中的条件将AttLeaveCount对应的数据库表的数据删除-->
    <delete id="deleteAttLeaveCount" parameterType="com.cd.common.Assist">
        delete from Att_Leave_Count
        <if test="require!=null"><include refid="Assist" /></if>
    </delete>

    <!-- 通过AttLeaveCount的id将AttLeaveCount的数据更新到数据库中对应的表,包括值null的数据-->
    <update id="updateAttLeaveCountById" parameterType="com.cd.basic.pojo.domain.AttLeaveCount">
        update Att_Leave_Count set
            USER_FK_CODE=#{userFkCode}
            ,LEAVE_TIME=#{leaveTime}
            ,LEAVE_TYPE=#{leaveType}
            ,DEL_STATUS=#{delStatus}
            ,LEAVE_DATE=#{leaveDate}
            ,CREATE_TIME=#{createTime}
            ,UPDATE_TIME=#{updateTime}
            ,SINGLE_FK_CODE=#{singleFkCode}
            ,FK_CODE=#{fkCode}
        where id=#{id}
    </update>

    <!-- 通过AttLeaveCount的id将AttLeaveCount中属性值不为null的数据更新到数据库对应的表中-->
    <update id="updateNonEmptyAttLeaveCountById" parameterType="com.cd.basic.pojo.domain.AttLeaveCount">
        update Att_Leave_Count
        <set>
            <if test="userFkCode != null">
                USER_FK_CODE=#{userFkCode},
            </if>
            <if test="leaveTime != null">
                LEAVE_TIME=#{leaveTime},
            </if>
            <if test="leaveType != null">
                LEAVE_TYPE=#{leaveType},
            </if>
            <if test="delStatus != null">
                DEL_STATUS=#{delStatus},
            </if>
            <if test="leaveDate != null">
                LEAVE_DATE=#{leaveDate},
            </if>
            <if test="createTime != null">
                CREATE_TIME=#{createTime},
            </if>
            <if test="updateTime != null">
                UPDATE_TIME=#{updateTime},
            </if>
            <if test="singleFkCode != null">
                SINGLE_FK_CODE=#{singleFkCode},
            </if>
            <if test="fkCode != null">
                FK_CODE=#{fkCode},
            </if>
        </set>
        where id=#{id}
    </update>

    <!-- 通过辅助工具Assist中的条件将AttLeaveCount中的数据更新到数据库对应的表中,包括值为null的数据-->
    <update id="updateAttLeaveCount" parameterType="map">
        update Att_Leave_Count
        <set>
                id=#{enti.id},
                USER_FK_CODE=#{enti.userFkCode},
                LEAVE_TIME=#{enti.leaveTime},
                LEAVE_TYPE=#{enti.leaveType},
                DEL_STATUS=#{enti.delStatus},
                LEAVE_DATE=#{enti.leaveDate},
                CREATE_TIME=#{enti.createTime},
                UPDATE_TIME=#{enti.updateTime},
                SINGLE_FK_CODE=#{enti.singleFkCode},
                FK_CODE=#{enti.fkCode},
        </set>
        <if test="assist.require!=null"><include refid="updateAssist" /></if>
    </update>

    <!-- 通过辅助工具Assist中的条件将AttLeaveCount中属性值不为null的数据更新到数据库对应的表中-->
    <update id="updateNonEmptyAttLeaveCount" parameterType="map">
        update Att_Leave_Count
        <set>
            <if test="enti.id != null">
                id=#{enti.id},
            </if>
            <if test="enti.userFkCode != null">
                USER_FK_CODE=#{enti.userFkCode},
            </if>
            <if test="enti.leaveTime != null">
                LEAVE_TIME=#{enti.leaveTime},
            </if>
            <if test="enti.leaveType != null">
                LEAVE_TYPE=#{enti.leaveType},
            </if>
            <if test="enti.delStatus != null">
                DEL_STATUS=#{enti.delStatus},
            </if>
            <if test="enti.leaveDate != null">
                LEAVE_DATE=#{enti.leaveDate},
            </if>
            <if test="enti.createTime != null">
                CREATE_TIME=#{enti.createTime},
            </if>
            <if test="enti.updateTime != null">
                UPDATE_TIME=#{enti.updateTime},
            </if>
            <if test="enti.singleFkCode != null">
                SINGLE_FK_CODE=#{enti.singleFkCode},
            </if>
            <if test="enti.fkCode != null">
                FK_CODE=#{enti.fkCode},
            </if>
        </set>
        <if test="assist.require!=null"><include refid="updateAssist" /></if>
    </update>

</mapper>